{
  "$comment": "Provider auth registry. Declarative only — this file NEVER contains credentials, only HOW to configure them. auth entries are tried in order: env = standalone user environment variable; oauth = this skill's own login/token store; keyless = works without any credential.",
  "providers": {
    "exa": {
      "label": "Exa",
      "auth": [
        { "type": "env", "var": "EXA_API_KEY", "signupUrl": "https://dashboard.exa.ai/api-keys", "note": "Free tier available; semantic search with own index." }
      ]
    },
    "tavily": {
      "label": "Tavily",
      "auth": [
        { "type": "env", "var": "TAVILY_API_KEY", "signupUrl": "https://app.tavily.com/home", "note": "Free tier ~1000 calls/month; agent-focused search API." }
      ]
    },
    "brave": {
      "label": "Brave Search",
      "auth": [
        { "type": "env", "var": "BRAVE_API_KEY", "signupUrl": "https://brave.com/search/api/", "note": "Free tier 2000 calls/month; classic web index." }
      ]
    },
    "firecrawl": {
      "label": "Firecrawl",
      "auth": [
        { "type": "env", "var": "FIRECRAWL_API_KEY", "signupUrl": "https://firecrawl.dev/", "note": "Self-hosted: also set FIRECRAWL_URL to your endpoint." }
      ]
    },
    "tinyfish": {
      "label": "TinyFish",
      "auth": [
        { "type": "env", "var": "TINYFISH_API_KEY", "signupUrl": "https://tinyfish.ai/", "note": "SERP-backed search API." }
      ]
    },
    "zai": {
      "label": "Z.AI",
      "auth": [
        { "type": "env", "var": "ZAI_API_KEY", "signupUrl": "https://z.ai/manage-apikey/apikey-list", "note": "Z.AI webSearchPrime MCP API key." }
      ]
    },
    "jina": {
      "label": "Jina Reader",
      "auth": [
        { "type": "env", "var": "JINA_API_KEY", "signupUrl": "https://jina.ai/reader/", "note": "s.jina.ai search endpoint; free tier with rate limits." }
      ]
    },
    "xai": {
      "label": "xAI (Grok)",
      "auth": [
        { "type": "env", "var": "XAI_API_KEY", "signupUrl": "https://console.x.ai/", "note": "xAI Responses API live search." },
        { "type": "env", "var": "XAI_OAUTH_TOKEN", "credentialKind": "env-token", "note": "Optional SuperGrok/X Premium+ bearer token for standalone use." },
        { "type": "oauth", "source": "xai", "auto": true, "guide": "run --login xai (device authorization; token stored in ~/.search-fusion/auth.json)" }
      ]
    },
    "gemini": {
      "label": "Gemini grounding",
      "auth": [
        { "type": "env", "var": "GEMINI_API_KEY", "signupUrl": "https://aistudio.google.com/apikey", "note": "Google AI Studio key; grounding search via generativeLanguage API." },
        { "type": "oauth", "auto": true, "guide": "run --login antigravity or --login gemini-cli (token stored locally in ~/.search-fusion/auth.json)" }
      ]
    },
    "openai": {
      "label": "OpenAI web search",
      "auth": [
        { "type": "oauth", "guide": "ChatGPT OAuth is a CLI-private flow (e.g. `omp /login openai-codex`); this skill does not pull it. No standalone env key exists for the web_search tool." }
      ]
    },
    "codex": {
      "label": "OpenAI Codex web search",
      "auth": [
        { "type": "oauth", "source": "codex", "auto": true, "guide": "run --login codex (device authorization; token stored in ~/.search-fusion/auth.json)" }
      ]
    },
    "kimi": {
      "label": "Kimi Code search",
      "auth": [
        { "type": "env", "var": "KIMI_SEARCH_API_KEY", "signupUrl": "https://platform.moonshot.ai/", "note": "Kimi Code Console search key — not the plain MOONSHOT_API_KEY." },
        { "type": "oauth", "source": "kimi", "auto": true, "guide": "run --login kimi (device authorization; token stored in ~/.search-fusion/auth.json)" }
      ]
    },
    "duckduckgo": {
      "label": "DuckDuckGo",
      "auth": [
        { "type": "keyless", "note": "No credential needed; best-effort HTML scraping, may break on anti-bot changes or regional blocking (unreachable from some networks even via proxy). Zero-config fallback when reachable." }
      ]
    }
  }
}
