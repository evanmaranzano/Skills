# Web Hub 用户偏好配置
# 首次运行 check-deps.mjs 时自动复制为 config.env（git ignored）。
#
# 默认浏览器：留空每次询问，设值固定使用
# 合法值：chrome / edge
WEB_ACCESS_BROWSER=
# CDP Proxy 端口（默认 3456）
CDP_PROXY_PORT=3456
# 闲置 Tab 自动关闭时间（毫秒，默认 15 分钟）
CDP_TAB_IDLE_TIMEOUT=900000
